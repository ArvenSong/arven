<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.net.arven.home.dao.FileDao">

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, path, show_name, real_name, creator, type, tag, create_time, update_time
    </sql>
    <select id="getFileByTag" resultType="cn.net.arven.common.entity.File">
        select <include refid="Base_Column_List"></include> from file where tag like CONCAT('%',#{tag},'%') ORDER BY
        RAND()
        <choose>
            <when test="limit">
                LIMIT #{limit}
            </when>
        </choose>
    </select>


</mapper>
